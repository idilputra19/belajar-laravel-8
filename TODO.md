# TODO for Creating Basic CSS

- [x] Add CSS reset to normalize browser defaults
- [x] Define typography styles (fonts, sizes, colors)
- [x] Add basic layout styles (body, headings, paragraphs)
- [ ] Verify the CSS file is properly linked in views (if needed)
- [ ] Test the website to ensure styles are applied
